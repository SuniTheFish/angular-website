import { CamelAddSpacesPipe } from './camel-add-spaces.pipe';

describe('CamelAddSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new CamelAddSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
